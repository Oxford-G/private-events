<h1>Sign Up</h1>
<%= form_with model: @user do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %> 
  <%= f.submit 'Sign up!', class:'btn btn-success' %>
  <%= link_to "Log In", new_session_path%>
<% end %>